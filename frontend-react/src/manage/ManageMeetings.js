import React from 'react'
import lookup from './../functions/lookup'

function ManageMeetings(props) {
    const [meetings, setMeetings] = useState(false);

    if(!useState) {
        // lookup('GET', )
    }
}

export default ManageMeetings;